# ROS 2

ROS 2 학습 기록.

## 구성

- `notes/` — 개념 정리
- `practice/` — 실습 기록. 실행 명령과 결과 화면을 함께 남긴다
- `src/` — `colcon build`로 빌드하는 ROS 2 패키지

실습에서 쓴 패키지는 `src/`에 두고, `practice/` 쪽에서는 경로로 가리킨다.

## 기록 방식

- 개념 파일은 훑은 범위 단위로, 실습 파일은 세션 단위로 나눈다
- 실행하지 않은 것은 적지 않는다
- 잘못 이해했다가 고친 부분은 지우지 않고 남긴다
